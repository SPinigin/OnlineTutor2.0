@model OnlineTutor2.ViewModels.ProfileViewModel
@{
    ViewData["Title"] = "Профиль";
    var user = Model.User;
}

<div class="container py-5">
    <div class="row">

        <div class="col-lg-4 mb-4">
            <div class="card shadow mb-4">
                <div class="card-body text-center">
                    <div class="mb-4">
                        <div class="bg-primary rounded-circle d-inline-flex align-items-center justify-content-center text-white"
                             style="width: 150px; height: 150px; font-size: 3rem;">
                            @user.FirstName.Substring(0, 1)@user.LastName.Substring(0, 1)
                        </div>
                    </div>

                    <h4 class="mb-1">@user.FullName</h4>
                    <p class="text-muted mb-1">@user.Age лет</p>
                    <p class="text-muted mb-3">
                        @if (User.IsInRole("Admin"))
                        {
                            <span class="badge bg-danger">
                                <i class="fas fa-shield-alt"></i> Администратор
                            </span>
                        }
                        else if (User.IsInRole("Teacher"))
                        {
                            <span class="badge bg-primary">
                                <i class="fas fa-chalkboard-teacher"></i> Преподаватель
                            </span>
                        }
                        else if (User.IsInRole("Student"))
                        {
                            <span class="badge bg-success">
                                <i class="fas fa-user-graduate"></i> Ученик
                            </span>
                        }
                    </p>

                    <hr>

                    <div class="d-grid gap-2">
                        <a asp-action="EditProfile" class="btn btn-primary">
                            <i class="fas fa-edit"></i> Редактировать профиль
                        </a>
                        <a asp-action="ChangePassword" class="btn btn-outline-secondary">
                            <i class="fas fa-key"></i> Сменить пароль
                        </a>
                    </div>
                </div>
            </div>


            <div class="card shadow">
                <div class="card-body">
                    <h6 class="text-muted mb-3">
                        <i class="fas fa-info-circle"></i> Дополнительно
                    </h6>
                    <div class="mb-2">
                        <small class="text-muted">
                            <i class="fas fa-calendar"></i>
                            На сайте с @user.CreatedAt.ToString("dd MMMM yyyy")
                        </small>
                    </div>
                    @if (user.LastLoginAt.HasValue)
                    {
                        <div class="mb-2">
                            <small class="text-muted">
                                <i class="fas fa-sign-in-alt"></i>
                                Последний вход: @user.LastLoginAt.Value.ToString("dd.MM.yyyy HH:mm")
                            </small>
                        </div>
                    }
                    <div>
                        <small class="text-muted">
                            <i class="fas fa-circle @(user.IsActive ? "text-success" : "text-danger")"></i>
                            @(user.IsActive ? "Аккаунт активен" : "Аккаунт заблокирован")
                        </small>
                    </div>
                </div>
            </div>
        </div>


        <div class="col-lg-8">

            <div class="card shadow mb-4">
                <div class="card-header bg-white">
                    <h5 class="mb-0">
                        <i class="fas fa-user"></i> Личная информация
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row mb-3">
                        <div class="col-sm-4 fw-bold text-muted">
                            <i class="fas fa-signature"></i> Имя:
                        </div>
                        <div class="col-sm-8">@user.FirstName</div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-sm-4 fw-bold text-muted">
                            <i class="fas fa-signature"></i> Фамилия:
                        </div>
                        <div class="col-sm-8">@user.LastName</div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-sm-4 fw-bold text-muted">
                            <i class="fas fa-envelope"></i> Email:
                        </div>
                        <div class="col-sm-8">
                            @user.Email
                            @if (user.EmailConfirmed)
                            {
                                <span class="badge bg-success ms-2">
                                    <i class="fas fa-check"></i> Подтвержден
                                </span>
                            }
                            else
                            {
                                <span class="badge bg-warning ms-2">
                                    <i class="fas fa-exclamation"></i> Не подтвержден
                                </span>
                            }
                        </div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-sm-4 fw-bold text-muted">
                            <i class="fas fa-phone"></i> Телефон:
                        </div>
                        <div class="col-sm-8">
                            @(user.PhoneNumber ?? "Не указан")
                        </div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-sm-4 fw-bold text-muted">
                            <i class="fas fa-birthday-cake"></i> Дата рождения:
                        </div>
                        <div class="col-sm-8">
                            @user.DateOfBirth.ToString("dd MMMM yyyy")
                            <small class="text-muted">
                                (@user.Age лет)
                            </small>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-sm-4 fw-bold text-muted">
                            <i class="fas fa-calendar-plus"></i> Дата регистрации:
                        </div>
                        <div class="col-sm-8">
                            @user.CreatedAt.ToString("dd MMMM yyyy")
                        </div>
                    </div>
                </div>
            </div>

            @if (User.IsInRole("Student"))
            {

                <div class="card shadow">
                    <div class="card-header bg-white">
                        <h5 class="mb-0">
                            <i class="fas fa-chart-line"></i> Моя успеваемость
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row text-center">
                            <div class="col-md-4 mb-3">
                                <div class="p-3 border rounded">
                                    <h3 class="text-primary mb-0">@Model.TotalTestsCompleted</h3>
                                    <small class="text-muted">Тестов пройдено</small>
                                </div>
                            </div>
                            <div class="col-md-4 mb-3">
                                <div class="p-3 border rounded">
                                    <h3 class="text-success mb-0">@Model.AverageScore.ToString("F1")%</h3>
                                    <small class="text-muted">Средний балл</small>
                                </div>
                            </div>
                            <div class="col-md-4 mb-3">
                                <div class="p-3 border rounded">
                                    <h3 class="text-warning mb-0">@Model.TotalPointsEarned</h3>
                                    <small class="text-muted">Баллов набрано</small>
                                </div>
                            </div>
                        </div>
                        <div class="text-center mt-3">
                            <a asp-controller="StudentTest" asp-action="History" class="btn btn-outline-primary">
                                <i class="fas fa-history"></i> Посмотреть историю
                            </a>
                        </div>
                    </div>
                </div>
            }

            @if (User.IsInRole("Teacher"))
            {

                <div class="card shadow">
                    <div class="card-header bg-white">
                        <h5 class="mb-0">
                            <i class="fas fa-chalkboard-teacher"></i> Моя статистика
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row text-center">
                            <div class="col-md-4 mb-3">
                                <div class="p-3 border rounded">
                                    <h3 class="text-primary mb-0">@Model.TotalTests</h3>
                                    <small class="text-muted">Создано тестов</small>
                                </div>
                            </div>
                            <div class="col-md-4 mb-3">
                                <div class="p-3 border rounded">
                                    <h3 class="text-success mb-0">@Model.TotalClasses</h3>
                                    <small class="text-muted">Активных классов</small>
                                </div>
                            </div>
                            <div class="col-md-4 mb-3">
                                <div class="p-3 border rounded">
                                    <h3 class="text-warning mb-0">@Model.TotalStudents</h3>
                                    <small class="text-muted">Учеников</small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
@section Scripts {
    <script src="~/js/layout.js" asp-append-version="true"></script>
}
